<div class="container-fluid">
   <p id="notice"><%= notice %></p>
   <div class="row">
      <div>
         <h1>Products</h1>
      </div>
   </div>
   <div class="row">
      <%#= @test_variable %> <!-- DELETE LATER-->
      <% @products.each do |product| %>
      <!-- <%#= render partial: "product", locals: {product: product} %> --> <!-- standard way --> 
      <%= render product %>                    
      <% end %>
   </div>
</div>
<br>
<%= link_to 'New Product', new_product_path %>